apiVersion: v1
kind: Pod
metadata:
  labels:
    app: whoami-pod
  name: whoami-pod-01
spec:
  containers:
    - image: jwilder/whoami
      name: whoami
      ports:
        - name: whoami-01-port
          containerPort: 8000
